# ==============================================================================
# Voice Agent Messenger - Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in the values
#
# 💡 TIP: Generate a secure JWT_SECRET with:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# ==============================================================================

# ==============================================================================
# 🔐 SECURITY - REQUIRED
# ==============================================================================
# IMPORTANT: Change this in production! This secret is used to sign JWT tokens
# for WebSocket authentication. Keep it safe and never commit to version control.
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ==============================================================================
# 🌐 CORS CONFIGURATION
# ==============================================================================
# Controls which domains can access the API (prevents CORS errors)
# Development: Use * to allow all origins
# Production: Set to your frontend domain (e.g., https://your-domain.com)
CORS_ORIGIN=*

# ==============================================================================
# 🤖 WHISPER SPEECH-TO-TEXT SERVICE (Port 8001)
# ==============================================================================
# Model size affects accuracy vs speed vs memory:
#   - tiny:   ~39 MB - Fastest, lowest accuracy
#   - base:   ~74 MB - Good balance (recommended for most cases)
#   - small:  ~244 MB - Better accuracy
#   - medium: ~769 MB - High accuracy
#   - large:  ~1.5 GB - Highest accuracy, slowest
WHISPER_MODEL_SIZE=base

# Device to run Whisper on:
#   - cpu:  Works everywhere, slower
#   - cuda: Requires NVIDIA GPU, much faster
WHISPER_DEVICE=cpu

# Compute type for quantization (affects speed and memory):
#   - int8:         8-bit integers, fastest, lowest memory
#   - int8_float16: Mixed precision, good balance
#   - float16:      Full precision, highest memory usage
WHISPER_COMPUTE_TYPE=int8

# ==============================================================================
# 🔊 PIPER TEXT-TO-SPEECH SERVICE (Port 5000)
# ==============================================================================
# Path to the Piper model file (.onnx format)
# Pre-downloaded models are in the /piper-models directory
# Available models include: en_US-amy-medium, en_US-amy-medium.onnx
PIPER_MODEL_PATH=/piper-models/en_US-amy-medium.onnx

# ==============================================================================
# 📊 LOGGING LEVEL
# ==============================================================================
# Controls verbosity of logs:
#   - debug: Most verbose, includes all details
#   - info:  Standard logging (recommended)
#   - warn:  Only warnings and errors
#   - error: Only errors
LOG_LEVEL=info

# ==============================================================================
# 🔴 REDIS (Managed by Docker Compose - Do Not Modify)
# ==============================================================================
# REDIS_URL is automatically set by docker-compose.yml
# Format: redis://:@localhost:6379
# No need to configure manually unless running outside Docker
