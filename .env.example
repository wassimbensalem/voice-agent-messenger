# Environment configuration template
# Copy this to .env and fill in the values

# Server Configuration
PORT=8080
NODE_ENV=production

# JWT Secret - SECURITY CRITICAL
# ================================================
# WARNING: Never use the placeholder value in production!
# 
# To generate a secure secret, run:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# Requirements:
# - Minimum 32 characters (64+ recommended)
# - Must contain random characters (use generated value)
# - Never commit to version control
# - Rotate periodically in production
# ================================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# CORS Configuration
# For production, restrict to your frontend domain:
# CORS_ORIGIN=https://your-frontend-domain.com
# Use * only for development
CORS_ORIGIN=*

# Heartbeat/Connection Settings
HEARTBEAT_INTERVAL=30000
ROOM_TIMEOUT=300000
MAX_PARTICIPANTS=10

# Redis Configuration (optional - for multi-instance support)
# Format: redis://[username:password@]host:port/database
# REDIS_URL=redis://localhost:6379

# Logging
# Levels: error, warn, info, debug, silly
LOG_LEVEL=info
